describe('days state', () => {
    xit('should create a new day', () => {

    });

    xit('should update a day if it exists', () => {

    });
});

describe('current week', () => {
    xit('should create a current week with seven days', () => {

    });

    xit('the week should contain today', () => {

    });
});

describe('current leak week', () => {
    xit('should update on new in leak', () => {

    });

    xit('should update on new out leak', () => {

    });

    xit('should update on updated leak', () => {

    });    

    describe('current squeeze week', () => {
        xit('should update on new squeeze', () => {

        });

        xit('should update on updated squeeze', () => {

        });
    });

    describe('current week percentages', () => {
        xit('should update percentage on upserted squeezes', () => {

        });

        xit('should update percentage on upserted leaks', () => {

        });
    });

    describe('current insights week', () => {
        xit('should be ordered by date descending', () => {

        });
    });
});


describe('settings', () => {

});
